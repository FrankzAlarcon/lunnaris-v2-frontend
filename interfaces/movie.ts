export interface FileMetadata {
  id: string
  tag: string
  type: string
}

export interface Poster {
  id: string;
  name: string;
  image: string;
}

export interface Movie {
  id: string;
  name: string;
  slug: string;
  image: string;
}